services:
  - type: web
    name: kcp-policy-dashboard
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    healthCheckPath: /api/health
    envVars:
      - key: KCP_FROM
        value: "202001"
      - key: KCP_TO
        value: "202212"
